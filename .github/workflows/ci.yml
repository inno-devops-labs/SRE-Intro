name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    if: ${{ !startsWith(github.event.head_commit.message, 'ci:') }}
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push gateway
        run: |
          IMAGE=ghcr.io/$(echo "${{ github.actor }}" | tr '[:upper:]' '[:lower:]')/quickticket-gateway:${{ github.sha }}
          docker build -t "$IMAGE" ./app/gateway
          docker push "$IMAGE"

      - name: Build and push events
        run: |
          IMAGE=ghcr.io/$(echo "${{ github.actor }}" | tr '[:upper:]' '[:lower:]')/quickticket-events:${{ github.sha }}
          docker build -t "$IMAGE" ./app/events
          docker push "$IMAGE"

      - name: Build and push payments
        run: |
          IMAGE=ghcr.io/$(echo "${{ github.actor }}" | tr '[:upper:]' '[:lower:]')/quickticket-payments:${{ github.sha }}
          docker build -t "$IMAGE" ./app/payments
          docker push "$IMAGE"

      - name: Update image tags in manifests
        run: |
          SHA=${{ github.sha }}
          ACTOR=$(echo "${{ github.actor }}" | tr '[:upper:]' '[:lower:]')
          sed -i "s|image: ghcr.io/.*/quickticket-gateway:.*|image: ghcr.io/${ACTOR}/quickticket-gateway:${SHA}|" k8s/gateway.yaml
          sed -i "s|image: ghcr.io/.*/quickticket-events:.*|image: ghcr.io/${ACTOR}/quickticket-events:${SHA}|" k8s/events.yaml
          sed -i "s|image: ghcr.io/.*/quickticket-payments:.*|image: ghcr.io/${ACTOR}/quickticket-payments:${SHA}|" k8s/payments.yaml

      - name: Commit and push manifest update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add k8s/gateway.yaml k8s/events.yaml k8s/payments.yaml
          git diff --cached --quiet || git commit -m "ci: update image tags to ${{ github.sha }}"
          git push
